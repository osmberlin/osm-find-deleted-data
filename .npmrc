# All dependencies come from the public npm registry. This also stops Dependabot
# from trying to use the org-level GitHub Packages registry (which it can't,
# without scope config) and failing the npm update job.
registry=https://registry.npmjs.org/
